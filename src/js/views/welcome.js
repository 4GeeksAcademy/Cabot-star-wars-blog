import React from "react";
import {Link, useParams} from "react-router-dom";
import {Contacts} from "../store/appContext";

export const welcome = props => {
    const {store, actions} = useContext(Context);
    const params = useParams();
    return(
        
    )
}